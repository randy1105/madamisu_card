<div class="main">
  <div class="side-box">
   <ul class="lists">
      <li class="list"><a href="#">通過一覧</a></li>
      <li class="list"><a href="#">カレンダー</a></li>
      <li class="list"><a href="#">お知らせ</a></li>
      <li class="list"><a href="#">シナリオ検索</a></li>
    </ul>
  </div>
  <div class="main-box">
    <div class="num">
      登録シナリオ数は<%= @scenarios.count %>話です。
    </div>
    <div class="scenarios">
      <div class="s-title">
        <div class="calm">
          タイトル
        </div>
        <% @scenarios.each do |scenario| %>
          <%= link_to scenario.title, "#" %>
        <% end %>
      </div>
      <div class="people">
        <div class="calm">
          人数
        </div>
        <% @scenarios.each do |scenario| %>
          <%= scenario.people.name %>
        <% end %>
      </div>
    </div>
  </div>
</div>